import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        main: "#A0ADC9",
      },
      secondary: {
        main: "#6F405E",
      },
      text: {
        primary: "#E5E9F0",
        secondary: "#212121",
      },
      background: {
        default: "#0C0F14",
      }
    }
});

export default theme;